# AI Integration Module - Learn Anything

This module handles calling the Google Gemini API to generate structured, validated JSON learning roadmaps.

## Setup

1. Install requirements:
   ```bash
   pip install -r ai/requirements.txt